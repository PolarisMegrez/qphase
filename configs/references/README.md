# Reference parameter assets

Files under this directory preserve scientifically important model parameter
sets independently of executable Workflow schemas. They are not accepted by
`qphase run` directly.

Each record identifies the perturbation parameter, critical value, source
Workflows, retained Sessions, and reports required to interpret the numbers.
Executable Workflows for a future QPhase version must be generated from these
records and validated against the cited evidence.
